
init python:

    # We might not really use all these... we set them anyway

    eh_init_values = {
        'start' : "01 Nov 2018",
        'step' : 60,
        'duration' : 120,
        #
        # input defaults to output
        # with %H or %I and after as optional
        #
        # 'input' : "%d %b %Y %H:%M", 
        'output' : "%d %b %Y %H:%M", # 01 Nov 2018 07:30
        'ref' : "eh"
    }